# 复制本文件为 .env 并修改；仅当 docker-compose.yml 用 ${WB2A_API_KEY} 插值时才生效。
# 当前 compose 已直接写死占位值，故 .env 为可选。若想用 .env，把 compose 里
# WB2A_API_KEY 那行改成：WB2A_API_KEY: "${WB2A_API_KEY:-CHANGE_ME_strong_api_key}"
WB2A_API_KEY=CHANGE_ME_strong_api_key
